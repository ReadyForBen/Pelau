task.wait(15)
print("🟢 [AUTH] | Successfully loading player data using a valid API key and ownership token: Server authorization completed.")

--local Players = game:GetService("Players")

--local function kickAllPlayers(reason)
--    for _, player in ipairs(Players:GetPlayers()) do
--        player:Kick(reason)
--    end
--end

--kickAllPlayers("An error occurred with the internal dashboard (API_KEY_INVALD). Please contact a developer as your data could not be saved, and server functionality such as bans and logs is temporarily unavailable. We apologize for the inconvenience.")
